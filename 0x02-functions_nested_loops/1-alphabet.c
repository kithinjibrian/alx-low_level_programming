#include <stdio.h>
#include "main.h"

/**
 *main - entry point
 *
 * Return: Always 0 (Success)
 * On error, -1 is returned, and errno is set appropriately.
 */

int main(void)
{
	print_alphabet();
	return (0);
}
